# School Connect Route Lock v1\n\n## Applied changes\n\n- Root / now safely redirects to /auth/login without using a missing Supabase import\n- Login now locks school users to /school, teacher users to /teacher, and parents to /feed\n- Signup route is marked: school goes to setup, parent goes to parent home /feed\n- FeedClient now waits for teacher-token detection before sending unauthenticated users to login\n- /feed is now role-locked: school -> /school, teacher-token -> /teacher, parent remains /feed\n- FeedClient prevents old feed UI from flashing for school/teacher users\n- /school now redirects non-school roles cleanly: teacher -> /teacher, parent -> /feed\n- TeacherSelfProfile no longer sends teacher users back to the parent /feed\n\n## Locked product flow\n\n- `/` opens `/auth/login`.\n- School users belong on `/school`.\n- Teacher users belong on `/teacher`.\n- Parent users belong on `/feed`.\n- `/feed` is now protected from school/teacher users so the old mixed feed does not flash as the wrong home.\n\n## Scope lock\n\n- No feed content/query changes.\n- No parent-child linking changes.\n- No message/composer changes.\n- No database/schema changes.\n- No page deletion yet.\n\n## Files changed\n\n- src/app/page.tsx\n- src/app/auth/login/page.tsx\n- src/app/auth/signup/page.tsx\n- src/components/feed/FeedClient.tsx\n- src/components/school/SchoolPageClient.tsx\n- src/components/teacher/TeacherSelfProfile.tsx\n